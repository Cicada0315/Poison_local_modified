<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
        <link href="/stylesheets/application.css" rel="stylesheet" type="text/css">
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
        
        <title>Poison</title>
    </head>
    <nav class="navbar navbar-expand-lg navbar-light nav-bar">
        <div class="container-fluid">
            <a href="/">
                <img src="/images/poison.png" alt="Poison" width="30" height="30">
            </a>
            <a class="nav-link" aria-current="page" href="/all" >Cocktails</a>
            <% if logged_in? %> 
                <a class="nav-link" href="/cocktails">My Cocktails</a>
                <a class="nav-link" href="/cocktails/new">New Cocktail</a>
                <a class="nav-link" href="/logout">Logout</a>
            <% else %>
                <a class="nav-link" href="/signup">Signup</a>
                <a class="nav-link" href="/login">Login</a>
            <% end%>
            <form class="d-flex" action="/result" method="get">
                <input class="form-control me-2" name="name" placeholder="Search by name">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
            </div>
        </div>
    </nav>
    <div class="text-white bg-dark">
        <%= flash[:error] %>
    </div>
    <%= yield %>
    
</html>